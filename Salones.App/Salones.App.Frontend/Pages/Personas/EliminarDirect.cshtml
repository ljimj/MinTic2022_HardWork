@page
@model Salones.App.Frontend.Pages.EliminarDirectModel
@{
}
<h1>Se va a eliminar el Directivo</h1>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Nombres</th>
            <th scope="col">Apellidos</th>
            <th scope="col">Tipo identificacion</th>
            <th scope="col">Numero de documento</th>
            <th scope="col">Edad</th>
            <th scope="col">Unidad</th>
            <th scope="col">Sintomas</th>
            <th scope="col">Tipo de Sintomas</th>
            <th scope="col">Estado Covid</th>
            <th scope="col">Fecha Diagnostico</th>
            <th scope="col">Periodo Aislamiento </th>
        </tr>
     </thead>
    <tbody>
        <tr>
            <td> @Model.directivo.nombres </td>
            <td> @Model.directivo.apellidos </td>
            <td> @Model.directivo.tipoIdentificacion </td>
            <td> @Model.directivo.identificacion </td>
            <td> @Model.directivo.edad </td>
            <td> @Model.directivo.unidad </td>
            <td> @Model.directivo.estadoCovid.sintomas</td>
            <td> @Model.directivo.estadoCovid.tipoSintomas</td>
            <td> @Model.directivo.estadoCovid.estadoCovid</td>
            <td> @Model.directivo.estadoCovid.fechaDiagnostico</td>
            <td> @Model.directivo.estadoCovid.periodoAislamiento</td>
            
        </tr>
       </tbody>
</table>
<form method="post">
    <input type="hidden" asp-for="directivo.id">
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Confirmar Eliminar</button>
        <a class="btn btn-primary table-tbn" asp-page="/Personas/ListDirect"> Volver </a>
    </div>
</form>